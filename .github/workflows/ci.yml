name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Install java-vectors to local Maven repo
        working-directory: ../java-vectors/vectors
        run: ./gradlew publishToMavenLocal -x test

      - name: Build + Test
        working-directory: models
        run: ./gradlew build

      - name: Generate SBOM
        working-directory: models
        run: ./gradlew cyclonedxDirectBom

      - name: Compliance Check
        working-directory: models
        run: ./gradlew complianceCheck

      - name: Upload SBOM
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: models/*/build/reports/cyclonedx-direct/bom.json
          retention-days: 90

      - name: Upload Test Reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: models/*/build/reports/tests/
          retention-days: 30
